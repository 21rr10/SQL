-- select distinct from where groupby having orderby
--distinct is an expensive operation --use wisely
--removes duplicates (repeated values) where each values appears only once
select distinct country
from customers;